CREATE TABLE public.svg_wireframes (id uuid PRIMARY KEY DEFAULT extensions.uuid_generate_v4(), screen_id uuid NOT NULL, svg_content text NOT NULL, screen_name text NOT NULL, created_at timestamp with time zone DEFAULT now(), updated_at timestamp with time zone DEFAULT now(), CONSTRAINT svg_wireframes_screen_id_fkey FOREIGN KEY (screen_id) REFERENCES public.screens(id) ON DELETE CASCADE);
